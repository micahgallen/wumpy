# MUD Persistence System - Data Flow Diagrams

**Visual representation of all data flows through persistence systems**

---

## Overview Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      MUD SERVER RUNTIME                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐     │
│  │   Players    │    │  Containers  │    │   Corpses    │     │
│  │   (Active)   │    │   (Active)   │    │   (Active)   │     │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘     │
│         │                    │                    │              │
│         └────────────────────┴────────────────────┘              │
│                              │                                   │
│                    ┌─────────▼────────┐                         │
│                    │  STATE MANAGER   │                         │
│                    │  (60s auto-save) │                         │
│                    └─────────┬────────┘                         │
└──────────────────────────────┼──────────────────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   DISK PERSISTENCE  │
                    │  (JSON Files)        │
                    └──────────┬──────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                       │
    ┌───▼────┐         ┌──────▼───────┐      ┌───────▼──────┐
    │players │         │  containers  │      │   corpses    │
    │ .json  │         │   .json      │      │    .json     │
    └────────┘         └──────────────┘      └──────────────┘
```

---

## Item Lifecycle & Location Tags

### Correct Flow (Ideal)

```
┌─────────────────────────────────────────────────────────────┐
│ ITEM LOCATION LIFECYCLE (CORRECT BEHAVIOR)                  │
└─────────────────────────────────────────────────────────────┘

1. Item spawns in room
   location: { type: 'room', roomId: 'sesame_street_01' }
         │
         │ Player: "get potion"
         ▼
2. Item in player inventory
   location: { type: 'inventory', owner: 'cyberslayer' }
         │
         │ Player: "put potion in chest"
         ▼
3. Item in container
   location: { type: 'container', containerId: 'chest_123' }  ✓ CORRECT
         │
         │ Server restart
         ▼
4. Item restored in container
   location: { type: 'container', containerId: 'chest_123' }  ✓ STILL CORRECT
```

### Actual Buggy Flow (Current)

```
┌─────────────────────────────────────────────────────────────┐
│ ITEM LOCATION LIFECYCLE (BUGGY BEHAVIOR)                    │
└─────────────────────────────────────────────────────────────┘

1. Item spawns in room
   location: { type: 'room', roomId: 'sesame_street_01' }
         │
         │ Player: "get potion"
         ▼
2. Item in player inventory
   location: { type: 'inventory', owner: 'cyberslayer' }
         │
         │ Player: "put potion in chest"
         ▼
3. Item in container (BUG: location not updated!)
   location: { type: 'inventory', owner: 'cyberslayer' }  ❌ WRONG!
         │
         │ Container saved to disk
         ▼
4. containers.json stores corrupted data
   location: { type: 'inventory', owner: 'cyberslayer' }  ❌ PERSISTED BUG!
         │
         │ Server restart
         ▼
5. Item restored in container with wrong location tag
   location: { type: 'inventory', owner: 'cyberslayer' }  ❌ CORRUPTION!
         │
         │ Player logs in
         ▼
6. Item may appear in BOTH places (duplication bug)
```

---

## Player Data Flow

### Login Sequence

```
┌──────────┐
│  Client  │
│  Socket  │
└────┬─────┘
     │ Username & Password
     ▼
┌────────────────────────────────────┐
│  AuthenticationFlow                │
│  handleLoginPassword()             │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  PlayerDB.authenticate()           │
│  - Load from players.json          │
│  - Verify password hash            │
│  - Return playerData object        │
└────┬───────────────────────────────┘
     │
     ├─────────────────────────────────┐
     │                                 │
     ▼                                 ▼
┌─────────────────┐          ┌────────────────────┐
│ Restore Stats   │          │ Restore Inventory  │
│ - STR, DEX, CON │          │ InventorySerializer│
│ - HP, MaxHP     │          │ .deserializeInv()  │
│ - Level, XP     │          │                    │
└────┬────────────┘          └────┬───────────────┘
     │                             │
     │      ┌──────────────────────┘
     │      │
     ▼      ▼
┌────────────────────────────────────┐
│  Player Object (In Memory)         │
│  - currentRoom: 'sesame_street_01' │
│  - inventory: [items...]           │
│  - stats, hp, equipment            │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  EquipmentManager                  │
│  .recalculatePlayerStats()         │
│  - Apply equipment bonuses         │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  SessionManager                    │
│  .registerSession()                │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  Player Enters Game World          │
└────────────────────────────────────┘
```

### Movement & Position Save

```
Player types: "north"
     │
     ▼
┌────────────────────────────────────┐
│  movement.js                       │
│  movePlayer(player, 'north', ...)  │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  Validate Exit Exists              │
│  world.findExit(roomId, direction) │
└────┬───────────────────────────────┘
     │ Exit found: 'sesame_street_02'
     ▼
┌────────────────────────────────────┐
│  Update Player Object              │
│  player.currentRoom = 'sesame_02'  │
└────┬───────────────────────────────┘
     │
     ▼  ⚠️ IMMEDIATE SAVE
┌────────────────────────────────────┐
│  PlayerDB.updatePlayerRoom()       │
│  - players[user].currentRoom = ... │
│  - this.save()  ← Write to disk    │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  players.json UPDATED              │
│  Position saved immediately        │
└────────────────────────────────────┘
```

### Player Disconnect

```
Socket 'end' or 'error' event
     │
     ▼
┌────────────────────────────────────┐
│  AuthenticationFlow                │
│  Socket event handler              │
└────┬───────────────────────────────┘
     │
     ▼  ⚠️ CRITICAL SAVE POINT
┌────────────────────────────────────┐
│  PlayerDB.savePlayer(player)       │
│  - Save ALL player state:          │
│    - currentRoom                   │
│    - inventory (serialized)        │
│    - stats, hp, level, xp          │
│    - equipment state               │
│    - currency                      │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  players.json UPDATED              │
│  All state persisted               │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  SessionManager.removeSession()    │
│  Player removed from active set    │
└────────────────────────────────────┘
```

---

## Container Data Flow

### Get Item from Container

```
Player: "get potion from chest"
     │
     ▼
┌────────────────────────────────────┐
│  get.js                            │
│  executeGetFromContainer()         │
└────┬───────────────────────────────┘
     │
     ├─────────────────────────────────┐
     │                                 │
     ▼                                 ▼
┌──────────────────┐         ┌────────────────┐
│ Find Container   │         │ Find Item      │
│ in room.items    │         │ in container   │
│ or room          │         │ .inventory     │
│ containers       │         │                │
└────┬─────────────┘         └────┬───────────┘
     │                             │
     └──────────┬──────────────────┘
                │
                ▼
┌────────────────────────────────────┐
│  ItemFactory.restoreItem()         │
│  Create item instance from data    │
└────┬───────────────────────────────┘
     │
     ▼  ⚠️ LOCATION TAG SET HERE
┌────────────────────────────────────┐
│  itemInstance.location = {         │
│    type: 'inventory',              │
│    owner: player.username          │
│  }                                 │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  InventoryManager.addItem()        │
│  Add to player.inventory[]         │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  Remove from container.inventory[] │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  InventorySerializer.serialize()   │
│  playerDB.updatePlayerInventory()  │
│  Save to players.json              │
└────────────────────────────────────┘
```

### Put Item in Container (BUGGY)

```
Player: "put potion in chest"
     │
     ▼
┌────────────────────────────────────┐
│  put.js                            │
│  execute()                         │
└────┬───────────────────────────────┘
     │
     ├─────────────────────────────────┐
     │                                 │
     ▼                                 ▼
┌──────────────────┐         ┌────────────────┐
│ Find Item in     │         │ Find Container │
│ player.inventory │         │ in room        │
└────┬─────────────┘         └────┬───────────┘
     │                             │
     └──────────┬──────────────────┘
                │
                ▼
┌────────────────────────────────────┐
│  putItemInRoomContainer()          │
└────┬───────────────────────────────┘
     │
     ▼  ❌ BUG: Pushes full item object with location tag!
┌────────────────────────────────────┐
│  containerObj.inventory.push(item) │
│                                    │
│  Item still has:                   │
│  location: {                       │
│    type: 'inventory',              │
│    owner: 'cyberslayer'            │  ← WRONG!
│  }                                 │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  InventoryManager.removeItem()     │
│  Remove from player inventory      │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  Container has item with WRONG     │
│  location tag stored in memory     │
└────┬───────────────────────────────┘
     │
     ▼  (60 seconds later OR shutdown)
┌────────────────────────────────────┐
│  StateManager.saveAllState()       │
│  or ShutdownHandler                │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  RoomContainerManager.exportState()│
│  Saves corrupted location tag      │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  containers.json                   │
│  {                                 │
│    "inventory": [{                 │
│      "location": {                 │
│        "type": "inventory",        │  ← BUG PERSISTED!
│        "owner": "cyberslayer"      │
│      }                             │
│    }]                              │
│  }                                 │
└────────────────────────────────────┘
```

---

## Server Lifecycle

### Startup Sequence

```
node server.js
     │
     ▼
┌────────────────────────────────────┐
│  ServerBootstrap.initialize()      │
└────┬───────────────────────────────┘
     │
     ├──────────┬─────────┬───────────┐
     │          │         │           │
     ▼          ▼         ▼           ▼
┌─────────┐ ┌──────┐ ┌────────┐ ┌────────┐
│ Load    │ │ Load │ │ Init   │ │ Init   │
│ Items   │ │ Shops│ │ Player │ │ World  │
│ Registry│ │      │ │   DB   │ │        │
└────┬────┘ └──┬───┘ └───┬────┘ └───┬────┘
     │         │         │          │
     └─────────┴─────────┴──────────┘
                │
                ▼
┌────────────────────────────────────┐
│  Restore Persisted State           │
│  ┌──────────────────────────────┐  │
│  │ restoreCorpseState()         │  │
│  │ - Load corpses.json          │  │
│  │ - Restore decay timers       │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │ restoreContainerState()      │  │
│  │ - Load containers.json       │  │
│  │ - Restore respawn timers     │  │
│  └──────────────────────────────┘  │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  checkRespawns()                   │
│  Spawn missing NPCs                │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  StateManager.start()              │
│  Begin 60-second auto-save loop    │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  Server Ready for Connections      │
└────────────────────────────────────┘
```

### Periodic Auto-Save (Every 60 seconds)

```
         StateManager Timer
               ▲
               │ Every 60 seconds
               │
      ┌────────┴────────┐
      │                 │
      ▼                 │
┌──────────────┐        │
│ saveAllState()│────────┘
└──────┬───────┘
       │
       ├────────────────┬────────────────┐
       │                │                │
       ▼                ▼                ▼
┌────────────┐   ┌────────────┐  ┌────────────┐
│Save Timers │   │Save Corpses│  │Save        │
│            │   │            │  │Containers  │
│timers.json │   │corpses.json│  │containers  │
│            │   │            │  │  .json     │
└────────────┘   └────────────┘  └────────────┘

⚠️ MISSING: Player positions NOT saved here!
   Players only saved on:
   - Movement (immediate)
   - Disconnect (immediate)
   - Manual savePlayer() calls
```

### Shutdown Sequence

```
SIGTERM / SIGINT / Ctrl+C
     │
     ▼
┌────────────────────────────────────┐
│  ShutdownHandler.handleShutdown()  │
└────┬───────────────────────────────┘
     │
     ├──────────┬─────────┬───────────┐
     │          │         │           │
     ▼          ▼         ▼           ▼
┌─────────┐ ┌──────┐ ┌────────┐ ┌────────┐
│ Stop    │ │ Stop │ │ Save   │ │ Save   │
│Ambient  │ │State │ │Timers  │ │Corpses │
│Dialogue │ │Mgr   │ │        │ │        │
└─────────┘ └──────┘ └────────┘ └────────┘
                │
                ▼
        ┌───────────────┐
        │ Save          │
        │ Containers    │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │ Save Shops    │
        │ (async)       │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │ process.exit()│
        └───────────────┘
```

---

## Race Conditions & Timing Issues

### Scenario 1: Rapid Movement + Crash

```
T=0s:  Player at Room A
       ↓
T=1s:  "north" → Move to Room B
       └→ updatePlayerRoom() triggered
          └→ fs.writeFileSync() QUEUED
       ↓
T=2s:  "east" → Move to Room C
       └→ updatePlayerRoom() triggered
          └→ fs.writeFileSync() QUEUED
       ↓
T=3s:  "south" → Move to Room D
       └→ updatePlayerRoom() triggered
          └→ fs.writeFileSync() QUEUED
       ↓
T=3.5s: SERVER CRASH! 💥
       ↓
Result: Which write completed? Unknown!
        Player might restore to A, B, C, or D

⚠️ ISSUE: Multiple rapid writes may not all complete
   before crash. No write confirmation.
```

### Scenario 2: Put Item + Auto-Save Race

```
T=0s:  Player puts item in container
       └→ Container updated in memory
          └→ item has wrong location tag!
       ↓
T=1s:  Player moves to another room
       ↓
T=59s: StateManager about to auto-save
       ↓
T=60s: AUTO-SAVE TRIGGERED
       └→ Container exported with bug
          └→ containers.json saved
       ↓
T=61s: SERVER CRASH 💥
       ↓
Result: Corrupted location tag persisted to disk!
```

---

## Data Corruption Flow

### How Corruption Spreads

```
                ┌─────────────────┐
                │  Initial State  │
                │  (Clean)        │
                └────────┬────────┘
                         │
                         │ Player puts item
                         ▼
                ┌─────────────────┐
                │  Memory State   │
                │  (Corrupted)    │  ← location tag wrong
                └────────┬────────┘
                         │
                         │ Auto-save or shutdown
                         ▼
                ┌─────────────────┐
                │  Disk State     │
                │  (Corrupted)    │  ← Bug persisted
                └────────┬────────┘
                         │
                         │ Server restart
                         ▼
                ┌─────────────────┐
                │  Restored State │
                │  (Corrupted)    │  ← Corruption propagates
                └────────┬────────┘
                         │
                         │ Player logs in
                         ▼
                ┌─────────────────┐
                │  Runtime State  │
                │  (Duplicated?)  │  ← Item may appear twice
                └─────────────────┘
```

---

## Fix Strategy Diagram

### Proposed Fix Flow

```
Player: "put potion in chest"
     │
     ▼
┌────────────────────────────────────┐
│  put.js                            │
│  putItemInRoomContainer()          │
└────┬───────────────────────────────┘
     │
     ▼  ✅ NEW: Sanitize item data
┌────────────────────────────────────┐
│  Create clean item data:           │
│  const itemData = {                │
│    instanceId: item.instanceId,    │
│    definitionId: item.definitionId,│
│    quantity: item.quantity,        │
│    ...                             │
│    location: {                     │
│      type: 'container',            │  ← CORRECT!
│      containerId: containerId      │
│    }                               │
│  }                                 │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  containerObj.inventory.push()     │
│  Pushes CLEAN data                 │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  Container saved with correct      │
│  location tags                     │
└────────────────────────────────────┘
```

---

## Validation & Detection

### Startup Validation (Proposed)

```
ServerBootstrap.restoreContainerState()
     │
     ▼
┌────────────────────────────────────┐
│  Load containers.json              │
└────┬───────────────────────────────┘
     │
     ▼
┌────────────────────────────────────┐
│  FOR EACH container:               │
│    FOR EACH item in inventory:     │
│      ✅ Validate location.type     │
│      ✅ Check location matches     │
│         container context          │
│      ✅ Repair if corrupted        │
└────┬───────────────────────────────┘
     │
     ├─── Valid ──→ Restore as-is
     │
     └─── Invalid ──→ Repair or Warn
                      ↓
              ┌──────────────┐
              │ Log warning  │
              │ Fix location │
              │ Continue     │
              └──────────────┘
```

---

**End of Diagrams**
